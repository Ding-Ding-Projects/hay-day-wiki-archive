{
  "schemaVersion": 1,
  "runtime": {
    "name": "Node.js",
    "versionConstraint": ">=22.13.0",
    "source": "https://nodejs.org/",
    "installScope": "user"
  },
  "projectDependencies": {
    "manifest": "package-lock.json",
    "installCommand": "npm ci --no-audit --no-fund",
    "integritySource": "package-lock.json"
  },
  "notes": [
    "The dependency fetcher never writes dependencies into the repository.",
    "The desktop runtime and Squirrel.Windows packaging tools are declared in package-lock.json.",
    "Code signing is disabled. The installer is intentionally unsigned and verified from its PE certificate table."
  ]
}
